{
  "name": "my-website",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "docusaurus": "docusaurus",
    "start": "docusaurus start",
    "build": "docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "generate:notes": "node src/bin/generate-notes-data.js",
    "generate:books": "node src/bin/gen-books.js",
    "new": "node ./src/bin/latestPostsListGenerator.js",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids",
    "rss:all": "node ./src/bin/generateFullRSS.js",
    "publish": "npm run generate:books && npm run generate:notes && npm run new && npm run build && npm run rss:all && GIT_USER=chun10124 npm run deploy && git add . && git commit -m \"Publish: update site content\" --allow-empty && git push origin main"
  },
  "dependencies": {
    "@docusaurus/core": "3.9.2",
    "@docusaurus/preset-classic": "3.9.2",
    "@easyops-cn/docusaurus-search-local": "^0.52.1",
    "@mdx-js/react": "^3.0.0",
    "clsx": "^2.0.0",
    "glob": "^11.1.0",
    "gray-matter": "^4.0.3",
    "moment": "^2.30.1",
    "prism-react-renderer": "^2.3.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "3.9.2",
    "@docusaurus/theme-search-algolia": "^3.9.2",
    "@docusaurus/types": "3.9.2",
    "gh-pages": "^6.3.0"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=20.0"
  }
}
